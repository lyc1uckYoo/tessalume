# Tessalume 1.2 发布检查表

## 发布前必须完成

- [x] 仓库根目录已加入 MIT License，README 中的“开源”表述与许可证一致。
- [x] `Tessalume.App.csproj`、README 与 CHANGELOG 的版本号一致。
- [x] 在干净工作区执行 `./一键构建EXE.ps1 -NoLaunch`，保持 0 警告、0 错误、全部检查通过。
- [x] 确认 `dist/portable-win-x64/` 中同时存在 `Tessalume.exe` 与 `SHA256SUMS.txt`，并重新计算 SHA-256 与清单一致。
- [x] 把 EXE 复制到全新可写目录，验证首次启动欢迎页不会自动应用主题或关闭 Codex。
- [x] 在隔离便携目录检查“开机自动启动”默认关闭，并通过启动项回归检查确认旧品牌启动项只被清理而不会自动迁移。
- [x] 用隔离目录中的 EXE 检查自动更新：下载校验、旧版备份、进程外替换、自动重启、结果提示、用户配置保留与临时文件清理全部正常。
- [x] 使用错误 SHA-256 和被占用的目标 EXE 演练失败路径，确认当前 EXE 与 `data/` 用户配置保持可用且错误说明清楚。
- [x] 通过发布构建回归检查验证文件夹导入、ZIP 导入、重复主题替换、删除主题和恢复内置主题。
- [x] 验证主题应用、亮暗切换、高级图像调节持久化与恢复默认外观的运行时和界面链路。
- [x] 从最终 EXE 导出 Codex 创作者工作区，运行脚手架、冻结几何检查与便携契约校验。
- [x] 检查主题创作页完整显示提示词示例并可连续手动复制；诊断页不包含剪贴板操作，`data/logs/` 日志正常轮换并可从页面打开。
- [x] 通过实机亮暗截图、小屏幕自适应回归检查与 Per-Monitor V2 清单校验检查主界面、浮窗、欢迎页、导入页、设置页与诊断页。

## GitHub Release

- [ ] 工作区干净，提交记录与 CHANGELOG 对应；创建 `v1.2.0` 标签。
- [ ] 上传固定名称的 `Tessalume.exe` 和 `SHA256SUMS.txt`，不要上传便携数据目录或个人主题草稿；自动更新依赖这两个资源名称。
- [ ] 发布说明列出首次引导、ZIP 导入、Codex 一句话创作、图像调节、自动更新、诊断恢复与已知限制。
- [ ] 明确说明当前 EXE 是否具有代码签名；未签名时提示首次下载可能出现 Microsoft Defender SmartScreen 警告。
- [ ] 从发布页重新下载文件并核对 SHA-256，再完成一次首次启动与主题应用冒烟测试。

## 哔哩哔哩公开测试

- [ ] 视频简介放置 Releases、问题反馈和主题制作指南链接。
- [ ] 演示“下载 → 首次启动 → 应用主题 → 亮暗切换 → 一句话创作 → 导入 → 自动更新”的完整路径。
- [ ] 说明主题与日志均在本地，遇到问题记录诊断页状态并附上必要截图后再提交 Issue。
