# Tessalume 安全与隐私

## 支持范围

公开发布后，只维护最新的 `1.4.x` 版本。报告问题前请先确认使用的是 Releases 页面中的最新构建。

## 本机主题与更新边界

- Tessalume 自带的主题运行时只连接本机回环地址，不提供账号、云同步、主题商店或远程主题下载。
- 软件不修改 Codex 安装包、`app.asar` 或 Codex 用户数据。
- 主题包中的 CSS、JavaScript 与本地资源用于构建 Codex 的视觉主题；导入器会检查包结构、路径、文件类型与大小，并拒绝远程 CSS 资源、越界路径和 ZIP 链接条目。
- 高级主题的 `theme.js` 会在当前 Codex 页面中运行以创建视觉组件和动效。Tessalume 不再要求逐主题信任确认，但结构与路径校验不能证明任意第三方脚本安全；请只导入自己制作、由自己的 Codex 工作区生成或来源明确的主题。
- 诊断日志只保存在 `data/logs/`，不会自动复制或上传；诊断页仅展示本机运行状态并提供日志目录入口。
- 用户数据备份只在用户选择的本地路径创建，默认包含设置与必要运行状态，可选包含用户导入主题；日志、更新下载、缓存、临时文件和内置主题会被排除。备份恢复会校验清单、路径、大小和逐文件 SHA-256，并在替换前保存当前状态快照。
- 备份 ZIP 可能包含收藏、图像参数、本机路径记录以及用户主题脚本，请像保护其他本地配置备份一样保存，不要把个人备份作为公开主题分享包上传。
- `.tessalume-look.json` 图像方案是最多 64 KB 的纯参数分享文件，不包含图片、脚本、主题 ID 或本机路径；导入器拒绝未知格式、未知字段和越界数值。它可以用于分享图像调节方案，但不能代替完整主题 ZIP。
- 创作者工作区安全升级只覆盖 Tessalume 管理的工具文件，写入前逐段检查工作区与备份路径并拒绝 Junction、符号链接等重解析点；`themes/` 用户项目不进入升级文件清单。
- 开启自动检查后，软件每次启动只请求一次本仓库的 GitHub Releases API；发现新版本时仅显示本地更新提示，用户点击并确认后才下载所选 Release 资源。软件不会上传主题、日志、Codex 数据、账号或设备标识，用户可在设置中关闭自动检查。
- 安装新版本前会校验 GitHub 资源摘要或 `SHA256SUMS.txt` 中的 SHA-256；更新助手只替换当前 `Tessalume.exe`，保留 `data/`、`themes/`、`Compatibility/`、`Templates/` 和旧 EXE 备份，更新后确认启动成功再清理临时文件。

## 报告安全问题

请优先使用仓库的 [Security Advisory 私密报告入口](https://github.com/lyc1uckYoo/tessalume/security/advisories/new)，不要在公开 Issue 中粘贴敏感信息、账号信息或未检查的完整日志。

普通缺陷请使用 [Bug 报告模板](https://github.com/lyc1uckYoo/tessalume/issues/new?template=bug-report.yml)，并附上复现步骤、诊断页状态和必要截图。
