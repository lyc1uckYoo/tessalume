# Tessalume 版本记录

## 2.1.1

- 修复 2.1.0 的连接性能回归：主题切换、显示偏好和图像工作台现在优先复用并快速验证当前活动端口，不再在每次操作时等待 9229 与 9340–9399 的完整扫描。
- 仅在当前端口失效或首次连接时执行端口发现；继续保留 Codex++ 9229 自动识别、第三方宿主优先端口与断线重连能力。
- 新增慢端口回归门禁，确保健康的已知端口立即返回，不会被无响应的候选端口拖慢。

## 2.1.0

- 图像工作台升级到 3.0：12 套内置主题的首页横幅、左栏图片和聊天背景全部改为版本化推荐值，不再把裁切、滤镜、遮罩和静态变换写死在主题 CSS；亮暗六个槽位可独立编辑，并始终保留主题原图作为无损基线。
- 新增按真实 Codex 页面尺寸校准的大画布编辑器，支持拖动、滚轮缩放、精确 CSS 长度、适合/填充/居中、原图按住对比、实时边界与离线编辑；撤销、重做和分级恢复覆盖连续手势，恢复操作不会误改其它区域或图片来源。
- 个性化运行时升级为稀疏覆盖与图片增量协议：参数变化不再重复传输六张图片，主题切换使用原子提交避免默认画面闪烁；本地图片、主题推荐值、动态偏好和响应式构图各自保持单一职责。
- 新增 Codex 宠物中心和首只内置宠物“飞行雪绒”：可预览 11 组动态动作，在当前用户 `.codex\pets` 内完成安装、更新、修复、备份恢复和受管卸载；不会运行独立桌宠进程，也不会读取聊天、账号或日志。
- 重整个性化页面与导航层级，移除容易混淆的跨区域复制、旧个人图像方案和体验方案工作流；保留直接、可撤销的当前槽位编辑，以及按主题保存的动效、字号和界面密度。
- 兼容规则升级到 3.0.4 / contract 4，修复原生进度行出现时输入框外圈发黑、主题切换短暂闪默认内容，以及新版 Codex 设置页不再显示聊天背景的问题。
- 改进 Codex 连接发现：应用主题前优先附加已有本机 Codex 页面，自动识别 Codex++ 默认端口 9229，并在“运行与诊断”提供可持久化的优先端口设置；不再因只扫描 9340–9399 而另行启动不带插件的 Codex。
- 2.0.2 的 Schema 5 设置会在启动时先保存原始快照，再原子迁移为 Schema 7；收藏、最近主题、亮暗图像参数、本地图片路径、显示偏好、创作草稿和工作区记录继续保留，已废弃的旧方案字段不再写回。
- 继续拆分个性化、图像运行时和宠物管理模块，并将发布门禁扩充到 99 项完整回归；发布构建同时验证 12 套主题契约、冻结几何、运行时脚本、兼容包、便携资源和 SHA-256。

## 2.0.2

- 新增莫宁·初星穹镜与西格莉卡·语义晨曦两套 Template 1.0 旗舰主题，内置主题扩充至 12 套；两套主题均提供完整亮暗素材、角色卡片、记忆组件、同步组件、输入框挂件和角色专属动效。
- 重制莫宁主题的首页横幅、左栏、聊天背景、角色卡、记忆卡与输入框挂件，修正左栏选中态、任务卡片动效、首页动效、卡片文案和暗色人物构图。
- 顶部任务标题框改为按“标题文字 + 主题标识”整体自适应：有空间时随内容自然增长，接近中间身份牌或右侧控件后才缩略；该行为已同步到全部 12 套内置主题。
- 修复卡提希娅与尤诺主题首页输入框内部在亮色模式下显示原生背景的问题，并补充标题自适应运行时、主题契约和响应式回归检查。

## 2.0.1

- 重构图像工作台的信息层级与操作区，完整显示当前区域、亮暗模式、图片来源和参数组；“重置”现在只作用于当前区域、当前模式和当前参数组，并保持可撤销。
- 区域参数复制不再携带图片来源，粘贴到首页横幅、左栏或聊天背景时会保留目标区域自己的本地图片，避免无意覆盖用户素材。
- 创作中心改为按工作区分别保存创作需求，首次使用不再预填示例角色；Schema 5 会安全迁移旧草稿，并始终保留“下一主题”草稿和原有用户数据。
- 修复创作引导状态与界面显示脱节的问题；已有项目使用精简状态条，仍可随时打开其他工作区或开始新主题，新主题需求与当前项目互不串联。
- 优化创作表单、流程路由与图像调节在 1080 宽度和亮暗模式下的排版，并补充真实 WPF 首帧、按钮裁切、重置、撤销、参数转移和草稿隔离回归验证。

## 2.0.0

- 修复从 1.x 旧更新器直接升级到 2.0 时，旧格式更新结果没有配置快照、导致上一版本恢复入口无法建立的问题；2.0 会在任何配置迁移前保存旧版可读取的 `ui-settings.json` 与 `state.json`，并将恢复信息持久化后再继续启动。
- 更新成功但恢复点确实无法建立时，完成弹窗会明确提示用户先导出备份，不再错误宣称上一版本恢复点已经可用；新增跨代升级、旧 Schema 恢复和越界路径拒绝回归测试。
- 兼容小包改为固定文件顺序、固定归档时间和显式 UTF-8 编码；相同源码重复构建会得到一致的 ZIP 与 SHA-256，并由自动化测试持续验证。
- 新增版本化页面兼容引擎：Codex 主页面、左栏、输入框、消息区和设置页等结构识别统一由兼容配置管理，主题包不再各自承担页面选择器兼容。
- 新增官方小型兼容补丁：自动更新可独立发现、双重 SHA-256 校验、原子安装和回滚兼容规则；补丁失败时自动恢复上一规则或软件内置基线，无需重新下载完整 EXE。
- 完整升级软件更新恢复链路：新 EXE 必须在启动后提交健康确认，否则更新助手会自动恢复旧版；成功更新会保留经校验的上一版 EXE，并可从“关于与数据”手动恢复。
- 更新、失败自动回滚和手动恢复现在把 EXE 与 `ui-settings.json` / `state.json` 的版本化快照作为同一恢复单元；旧版启动前恢复其可读取的配置，较新配置另存本地恢复副本，收藏、主题、个人图片与创作项目不受影响。
- 新增 GitHub Actions 自动化：主分支和 PR 执行完整 Windows Release 构建；`vX.Y.Z` 标签校验项目版本与 CHANGELOG 后发布正式 EXE；`compat-vX.Y.Z` 标签只发布非 latest 的兼容补丁。
- 个性化 2.0 支持首页横幅、左栏图片和聊天背景的亮暗独立本地图片、构图、滤镜、叠色、渐变、暗角、混合模式与可读性保护；动效强度、字号和界面密度可以保存为完整体验方案，主题更新后继续保留个人参数。
- 创作中心 2.0 拆为工作区、创作流程、项目体检、运行验收和发布清单五个独立路由；新增五阶段进度、项目级 Codex 修复提示词和亮色/暗色/输入框/消息框/响应式布局批量验收，并明确区分主题项目问题与运行时兼容问题。
- 响应式运行验收会真实切换 800 / 1200 / 1800 三档视口后再恢复原窗口；警告状态不再被计为通过，任何未完成验收都会阻止导出分享包。
- 发布清单新增运行验收门禁；只有主题契约、素材、亮暗覆盖、静态体检和批量运行验收共同通过后才允许导出分享 ZIP。
- 完成模块化单体重构：诊断、关于、个性化和创作中心形成独立视图与应用服务；创作中心按 Domain / Application / Infrastructure / Presentation 分层，更新引导器、备份服务、主题扫描器和兼容运行时按单一职责拆分，构建与安装阶段再按固定清单组装运行时产物。

## 1.4.1

- 修复 Codex 更新原生输入框 DOM 后，主题输入框背景、描边、光效与内部控件样式失效并退回深色原生阴影的问题。
- 共享兼容运行时现在同时识别新旧输入框根节点与底部按钮栏，所有现有主题无需逐个修改即可恢复样式和输入框挂件定位。
- 增加针对当前 Codex 输入框结构的运行探针与回归检查，确认修复后的兼容标识、主题背景和按钮栏样式在实际运行页面中生效。

## 1.4.0

- 全面重整主界面视觉系统：首页、左侧导航、主题详情、创作中心、首次引导、顶部浮窗与产品弹窗统一使用更清晰的排版、色彩、间距和稳定悬停反馈，同时完成亮色与暗色适配。
- 个性化设置改为“主题与模式 → 图像区域 → 参数类型”的聚焦流程，一次只显示首页横幅、左栏图片或聊天背景中的一个编辑器；快捷编辑与个人方案改成独立彩色工具面板，减少信息密度与无效留白。
- 开机自启动与自动更新移动到“关于与数据 → 应用行为”；关于页同步展示 1.4 版本能力、便携数据、备份恢复和应用行为，产品设置边界更加明确。
- 运行与诊断改为先进入页面并呈现加载状态，再异步读取本机进程、回环端口、主题包与兼容性信息；首次冷启动的主题扫描与预览解码移出 UI 线程，设置页无需等待即可交互。

- 创作中心新增角色定制提示词生成器：可填写作品、角色、视觉方向、参考图和额外要求，自动生成包含角色研究、11 张素材确认、亮暗覆盖、Template 1.0 契约与最终校验的完整 Codex 提示词；草稿仅保存在本机并在退出前补写最后一次修改。
- 创作者工作区新增机器可读版本契约与明确状态提示，可识别旧版、可升级、当前版、更新版和损坏状态。
- 新增创作者工作区安全升级：只更新 Tessalume 管理的 Skill、Schema、说明和模板共享文件，覆盖前备份到 `.tessalume-backups`，始终不读取、删除或覆盖 `themes/` 中的用户项目。
- 发布前加固更新与输入边界：红色更新提示在确认、下载和失败恢复期间保持单任务互斥；图像精确输入拒绝 `NaN`/Infinity；工作区安全升级逐段拒绝受管理路径和备份路径中的 Junction 或符号链接。
- 主题画廊升级为 Theme Library 2.0：新增亮色/暗色大图并排详情、主题说明、作者、包 ID、Template、文件数量/体积与本地目录信息；可从选中栏直接查看、打开目录或应用主题。
- 新增默认、最近使用、名称和作者四种主题排序；最近使用时间与次数写入便携配置，重启后保持，同时继续兼容收藏、图像参数和旧版本配置。
- 主题文件夹和 ZIP 压缩包现在可直接拖入画廊导入；同 ID 主题覆盖前会比较现有/导入版本，明确区分升级、同版覆盖、降级与未知格式，并说明收藏和个性化配置不会被替换。
- 高级图像调节升级为可逆编辑器：支持会话级撤销/重做、按住对比主题原图、区域参数复制粘贴、亮暗整套参数复制、精确数值输入与方向键微调。
- 新增个人图像方案：可保存当前亮色或暗色下三个区域的完整参数，并在不同主题间复用、覆盖或删除；最多保留 24 套本地方案。
- 个人图像方案支持独立导入和导出 `.tessalume-look.json` 分享文件；文件只包含三个区域的数值参数，不携带图片、脚本、主题 ID 或本机路径，导入时校验格式版本、文件大小、未知字段与参数范围，同名方案替换前明确确认。
- 界面配置升级到 `SchemaVersion: 3`，显式迁移 0/1/2 版本；迁移时先保存原始 JSON 快照，再原子写入新配置，原有收藏、主题参数和创作工作区保持不变。
- 开启自动更新后改为每次启动检查一次；发现版本时在品牌区显示非打扰式红色更新提示，用户点击后才进入确认、下载和安装流程。
- 统一提示弹窗改为内容自适应布局：短消息不再保留大块空白，长更新说明继续使用固定标题/操作区与内部滚动正文。
- 高级图像调节新增构图与效果控制：缩放、水平/垂直位置、灰度、色相和柔化均支持首页横幅、左栏图片、聊天背景，并按主题及 Codex 亮暗模式分别持久化。
- 图像编辑器重构为“基础 / 构图 / 效果”三组可复用控件；新增变换以非破坏方式叠加在主题原始裁切之上，不修改主题文件或 Template 1.0 几何。

## 1.3.0

- 将静态“主题创作”页升级为独立创作项目中心：支持创建、打开、重新定位和移除工作区，记录最近工作区，并自动发现 `themes/` 下的主题项目。
- 新增结构化主题体检，按清单、入口文件、11 张素材、亮暗预览、Template 1.0、CSS、脚本和资源引用分组显示错误、警告、通过项及修复建议。
- 新增本地开发会话：监听项目声明文件，连续保存、原子替换和大图片写入稳定后再重新体检；支持手动重新应用、Codex 亮暗预览和默认关闭的会话级自动应用。
- 新增确定性主题 ZIP 导出：导出前二次校验，排除日志、缓存、临时文件和设计源文件，原子替换目标包，并显示文件数、大小和 SHA-256；导出结果会由 Tessalume 回读验证可重新导入。
- “关于与数据”新增便携备份与恢复。默认备份设置和运行状态，可选包含用户主题；恢复前显示摘要并自动快照当前数据，恢复失败时事务回滚，且禁止备份覆盖内置主题。
- 配置升级到 `SchemaVersion: 2`，保留 0/1 → 2 显式迁移；迁移前将原始设置保存到 `data/backups/latest-before-preferences-migration.json`。
- 新增兼容性健康记录：保存 Codex 版本、主题运行时契约、最近成功时间和失败阶段；Codex 或运行时变化后自动预检，任一 Codex 页面注入失败时统一清理全部目标，避免半注入状态。
- 诊断页新增兼容性基线和针对性恢复建议；诊断内容继续只保存在本机，不复制报告，也不上传日志。
- 重构创作、备份、兼容性和运行时边界，并将产品级自动检查扩充到 53 项；创作中心、关于数据页和兼容性诊断页均完成亮暗与滚动渲染检查。
- 自动更新继续只替换 `Tessalume.exe`，1.2.0/1.2.1 的 `data/`、`themes/`、收藏、图像参数和工作区记录升级后保持完整。

## 1.2.1

- 将主窗口、个性化设置、主题库、创作中心、更新、诊断和顶部浮窗按职责拆分为可独立维护的代码文件，保持原有功能与界面行为不变。
- 修复首次欢迎页版本徽标硬编码为 `v1.2` 的问题，改为始终显示当前程序集版本。
- 修复自动更新等长文本确认弹窗正文被截断、无法滚动以及右上角关闭按钮随内容跑偏的问题；标题栏、滚动正文和底部操作区现在保持独立布局。
- 将主窗口公共样式提取为独立资源字典，并删除不再使用的重复诊断窗口，降低后续界面修改的回归风险。
- 为便携界面配置加入显式 `SchemaVersion` 和 0 → 1 迁移，保留已有亮暗模式、收藏、自动更新设置与主题图像参数。
- 重整自动化测试工程，直接引用正式 App/Core 项目，并新增源码边界、WPF 拆分资源加载和配置迁移回归检查。
- 将本地维护文档与 GitHub 公共资源彻底分离；README 截图迁移到 `.github/assets/`，公开构建不再依赖本地 `docs/`。
- 保持自动更新只替换 `Tessalume.exe`，不改动同目录的 `data/`、`themes/`、`Compatibility/` 和 `Templates/`。

## 1.2.0

- 新增完整自动更新：默认定期检查官方 GitHub Releases，支持手动检查、版本说明、下载进度、SHA-256 校验、旧版备份、进程外安全替换、失败保留与自动重启。
- 发布前补强旧配置空值兼容、界面设置并发保存与更新网络错误中文提示，网络失败不会影响当前版本。
- 主题创作页改为直接展示更清晰的完整提示词示例，并提供单次、非弹窗的手动复制按钮；准备工作区不再自动写入剪贴板，诊断页也不再复制报告。
- 开机自动启动保持默认关闭；旧品牌启动项只做清理，不再自动迁移成 Tessalume 启动项，必须由用户主动开启。
- 项目源码正式采用 MIT License；第三方角色名称、形象与美术素材仍归原权利人所有。
- 新增首次启动欢迎引导；第一次运行不再随机应用主题，也不会未经确认关闭正在运行的 Codex。
- 应用主题需要重新启动 Codex 时，先提醒用户保存当前工作并明确确认。
- 移除主题信任记录与相关授权流程，继续保留主题结构、路径和本地资源校验。
- “主题创作”页可导出自包含的 Codex 创作者工作区，并直接展示一句话创作示例；工作区内置 Skill、Template 1.0、11 张素材规范和校验脚本。
- 诊断页新增本机状态总览、限额轮换日志与内置主题自助恢复；所有诊断内容仅保存在本地。
- 主题导入新增 ZIP 压缩包入口，支持根目录或单主题文件夹结构，并限制文件数量、解压大小、符号链接与越界路径。
- 构建产物自动生成 `SHA256SUMS.txt`；新增安全说明、Bug 报告模板和公开测试发布检查表。
- 主界面新增小屏幕等比适配与 Per-Monitor V2 DPI 支持；补充键盘快捷键、可见焦点和图标按钮/图像滑块的无障碍名称。
- 统一主界面、按钮、状态卡片、设置页和诊断页的视觉与中文文案。
- 主题画廊新增名称、说明、作者和主题 ID 搜索，以及亮色/暗色能力筛选与结果计数。
- 新增与主界面亮暗模式一致的确认、警告和错误弹窗；常用操作增加轻量完成提示。
- 新增“关于与数据”页面，集中显示当前版本、版本亮点、程序目录、便携数据目录和主题库概况。
- 首页横幅、左栏图片与聊天背景的亮度、对比度、饱和度和不透明度支持按主题及 Codex 亮暗模式分别持久化；默认保持原图。
- 内置主题清单更新为 10 款 Template 1.0 旗舰主题。

## 1.1.0

- 完成 Tessalume 品牌和工程结构迁移。
- 改进主题运行状态、自启动迁移和热切换生命周期。
- 主题运行继续限定在本机回环连接，不修改 Codex 安装文件。
