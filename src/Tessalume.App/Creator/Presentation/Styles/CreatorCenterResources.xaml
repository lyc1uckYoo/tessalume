<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="CreatorListBox" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    </Style>
    <Style x:Key="CreatorListItem" TargetType="ListBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,7" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="ItemChrome" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource HomeToolbarBorder}" BorderThickness="1" CornerRadius="12">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource HoverSurface}" /><Setter TargetName="ItemChrome" Property="BorderBrush" Value="{DynamicResource Accent}" /></Trigger>
                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource AccentSoft}" /><Setter TargetName="ItemChrome" Property="BorderBrush" Value="{DynamicResource Accent}" /></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ItemChrome" Property="Opacity" Value="0.55" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CreatorStatusPill" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource TealSoft}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Teal}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="7,3" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding StatusTone}" Value="warning"><Setter Property="Background" Value="{DynamicResource AmberSoft}" /><Setter Property="BorderBrush" Value="{DynamicResource Amber}" /></DataTrigger>
            <DataTrigger Binding="{Binding StatusTone}" Value="error"><Setter Property="Background" Value="{DynamicResource DangerSoft}" /><Setter Property="BorderBrush" Value="{DynamicResource Danger}" /></DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CreatorStatusText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Teal}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding StatusTone}" Value="warning"><Setter Property="Foreground" Value="{DynamicResource Amber}" /></DataTrigger>
            <DataTrigger Binding="{Binding StatusTone}" Value="error"><Setter Property="Foreground" Value="{DynamicResource Danger}" /></DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CreatorChecklistIcon" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource AccentSoft}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding StatusTone}" Value="warning"><Setter Property="Background" Value="{DynamicResource AmberSoft}" /></DataTrigger>
            <DataTrigger Binding="{Binding StatusTone}" Value="error"><Setter Property="Background" Value="{DynamicResource DangerSoft}" /></DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CreatorChecklistIconText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Accent}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding StatusTone}" Value="warning"><Setter Property="Foreground" Value="{DynamicResource Amber}" /></DataTrigger>
            <DataTrigger Binding="{Binding StatusTone}" Value="error"><Setter Property="Foreground" Value="{DynamicResource Danger}" /></DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="CreatorButtonBase" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
        <Setter Property="Background" Value="{DynamicResource Surface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource KeyboardFocusVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Chrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{DynamicResource HoverSurface}" /><Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Accent}" /></Trigger>
                        <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.76" /></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Chrome" Property="Opacity" Value="0.42" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CreatorMiniButton" TargetType="Button" BasedOn="{StaticResource CreatorButtonBase}">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Height" Value="30" />
        <Setter Property="MinWidth" Value="54" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="FontSize" Value="10" />
    </Style>
    <Style x:Key="CreatorDevButton" TargetType="Button" BasedOn="{StaticResource CreatorButtonBase}">
        <Setter Property="Height" Value="34" />
        <Setter Property="MinWidth" Value="84" />
        <Setter Property="Padding" Value="13,0" />
        <Setter Property="FontSize" Value="10.5" />
    </Style>
    <Style x:Key="CreatorTextBox" TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
        <Setter Property="Background" Value="{DynamicResource Surface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="11,8" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="CaretBrush" Value="{DynamicResource Accent}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource AccentSoft}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource KeyboardFocusVisual}" />
    </Style>
    <Style x:Key="CreatorSwitch" TargetType="ToggleButton">
        <Setter Property="Width" Value="42" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Background" Value="{DynamicResource Border}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="SwitchTrack" Width="42" Height="24" Background="{TemplateBinding Background}" CornerRadius="12" BorderBrush="{DynamicResource Border}" BorderThickness="1">
                        <Ellipse x:Name="SwitchThumb" Width="18" Height="18" Fill="White" HorizontalAlignment="Left" Margin="2,0,0,0" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True"><Setter TargetName="SwitchTrack" Property="Background" Value="{DynamicResource Accent}" /><Setter TargetName="SwitchTrack" Property="BorderBrush" Value="{DynamicResource Accent}" /><Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" /><Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,2,0" /></Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="SwitchTrack" Property="Opacity" Value="0.42" /></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CreatorRouteButton" TargetType="Button" BasedOn="{StaticResource CreatorButtonBase}">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Height" Value="42" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Style.Triggers>
            <Trigger Property="Tag" Value="active"><Setter Property="Background" Value="{DynamicResource AccentSoft}" /><Setter Property="BorderBrush" Value="{DynamicResource Accent}" /><Setter Property="Foreground" Value="{DynamicResource Accent}" /></Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
