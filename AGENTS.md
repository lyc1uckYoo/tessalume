# Codex Theme Studio 主题工作规范

## 工作范围

- 只处理用户点名的主题、页面和组件，不自行扩大修改范围。
- 修改前先把目标区域映射到准确的 CSS 选择器或脚本逻辑，避免混淆首页横幅、左栏、聊天背景、消息气泡、记忆卡和右下角动效。
- 除主题特有的角色图片、配色、符号、气泡和动效外，默认以当前 `xin.moonfox-sovereign` 为布局、卡片尺寸和聊天透明效果基准。
- 用户指定其他参考主题时，以当次指定为准；不要把不同角色做成重复姿态、重复构图或通用装饰。

## 文件同步与构建

- 每次主题修改必须同时更新源码与便携版对应文件：
  - `themes/...`
  - `dist/portable-win-x64/themes/...`
- 涉及便携版受信主题时，同步维护 `dist/portable-win-x64/data/trusted-themes.json` 中对应的主题指纹。
- 默认不构建。只有用户明确要求构建、重新打包或启动构建产物时才执行构建。
- 不得用构建覆盖用户已有的便携版改动。

## 视觉规范

- 同时考虑亮色与暗色表现，分别检查人物位置、大小、透明度、文字对比度和组件遮挡。
- 有专属聊天背景时，正文消息气泡、助手消息、用户消息以及编辑文件摘要等遮挡背景的容器默认使用全透明背景；边框、文字和必要状态仍须清晰可读。
- 亮色聊天正文使用纯黑或足够深的文字；暗色正文保持清晰对比。
- 角色主体应突出但不能遮挡正文；横幅、左栏、聊天页和右下角图片应各自使用适合该区域的构图。
- 动效应贴合角色主题，并控制位置和幅度，不能与卡片、文字或其他组件重叠。

## 图片与流量

- 最终图片素材保持高质量，不以省流量为由降低生成或交付质量。
- 同一张原图尽量只读取一次；后续分析、定位和预览优先使用压缩或缩小副本。
- 不反复打开包含大量旧图片的历史任务，不依赖旧任务继续传递图片上下文。
- 图片任务完成后，建议新开任务处理下一个主题；新任务只携带当前主题所需信息。
- 避免连续截图式 UI 验证。通常只保留最终亮色、暗色关键截图；只有定位问题确有需要时才增加中间截图。
- 使用程序、浏览器调试协议或项目现有截图方式完成视觉验证，不使用 Computer Use。

## 验证与交付

- 不构建时至少检查：
  - `manifest.json` 和其他 JSON 可解析；
  - `theme.js` 语法；
  - CSS 大括号与关键选择器；
  - 主题资源引用存在；
  - 源码与便携版对应文件一致；
  - 受信主题指纹正确。
- 视觉任务在条件允许时进行一次实际运行截图检查；若只完成静态检查，必须明确说明验证边界。
- 不把静态校验、运行验证和构建验证混为一谈。
- 完成后简洁说明修改范围、源码/便携版同步情况、是否构建，以及实际执行的验证。
